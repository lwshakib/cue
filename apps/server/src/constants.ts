/**
 * Enum for User Roles.
 */
export enum UserRoleEnum {
  ADMIN = "ADMIN",
  USER = "USER",
}

/**
 * Enum for Email Sending Types.
 */
export enum SendMailEnum {
  VERIFY_EMAIL = "VERIFY_EMAIL",
  RESET_PASSWORD = "RESET_PASSWORD",
}
